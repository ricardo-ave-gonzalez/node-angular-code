Middleware para que sirve ?
Seguridad basada en middleware
Manejo de errores a traves de middleware

Seguridad:
   Muy basico   : Cookies
   menos basico : Token pero tipo UUID - Se persiste en redis
   intermedio   : JWT (Autovalidable) - Se puede persistir en redis
   Nivel pro    : Oauth2 Framework de autenticacion.

PM2 que es para armar un cluster de procesos en node

child_process

log4js Aca podriamos meter algun de investigacion para centralizar logs

Deploy: 
   Nivel Manual: instalar en ubuntu nodejs, copiar los archivos
   Mivel Intermedio: hacerlo desde github el deploy
   Nivel robot: Todo automatizado. Cuando alguien mete un cambio, se
                Se hace el deploy automatico.

   Utilzando jenkins

Docker que es par que sirve como hacer deploy todo.

Balanceo de carga - NO se hace con node.
F5 o F10 hacen balanceo de carga

Proxy reverso, que es, que funcion cumple, etc.
nginx, kong.

Microservicios

    dos tipos a) Uno distribucion en diferentes apis.
              b) Mas sofisticado. desde el front hasta la base de datos.
                 por cada servicio.

    